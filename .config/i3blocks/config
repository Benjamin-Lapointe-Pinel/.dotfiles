command=$BLOCK_NAME/$BLOCK_NAME

[hostname]
command=hostnamectl hostname
interval=once

[bandwidth-led]
glyph=↓
metric=rx
format=json
interval=persist
separator=false
separator_block_width=0

[bandwidth-led]
glyph=↑
metric=tx
format=json
interval=persist
separator=false
separator_block_width=5

[iface]
interval=1
separator=false

[wifi]
label= 
INTERFACE=wlp2s0
interval=1
command=[[ -n "${BLOCK_BUTTON}" ]] && [[ "${button}" -eq 1 ]] && wpa_gui; $BLOCK_NAME/$BLOCK_NAME

[disk]
label= 
interval=60
separator=false

#TODO: migrate to led only
[disk-io]
interval=persist
WIDTH=4
INLABEL=↓ 
OUTLABEL=↑ 

[disk-led]
glyph=↓
metric=rx
format=json
interval=persist

[cpu_usage]
label= 
interval=1
DECIMALS=0
T_WARN=75
T_CRIT=90
separator=false

[memory]
label= 
interval=1

[battery2]
interval=5
separator=false

[temperature]
interval=5
separator=false

[backlight]
label= 
interval=20
STEP_SIZE=5
separator=false

#TODO: detect volume change with pactl
#TODO: also use this label: 婢
[volume]
label= 
STEP=5%
interval=once
signal=10

[keyboard-layout]
label= 
command=xkb-switch | tr a-z A-Z; xkb-switch -W | stdbuf -oL tr a-z A-Z
interval=persist
separator=false

[rofi-calendar]
LABEL= 
BAR_POSITION=top
WEEK_START=sunday
interval=1
DATEFTM=+%Y-%m-%d
SHORTFMT=+%Y-%m-%d
separator=false

[time]
label= 
command=date '+%H:%M:%S '
interval=1
